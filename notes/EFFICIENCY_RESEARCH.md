# Efficiency research

_Updated 2026-09-07 22:52:29_ · **Efficiency Research Agent**

> **Focus:** Per-worker yield, hot paths, latency, queue discipline, pre-dispatch — findings feed `notes/RESEARCH_SYNC.md`, `automation_improve`, and peer orchestrator prompts.

## This cycle

- probed 1 finding(s); 0 new

## Open findings


## Agent notes

- **2026-09-07 (peer-1 / efficiency_researcher L53)** — Assignment Port deferred_poison land-proof + ASN Lane E noop/compact. Evidence: hub `tests.test_compact_land_proof` FAIL (`_land_proof("deferred_poison")` False); vault L144–155; scrub live in `peer_transcript.py`. Landed: port vault branch into hub+peer-1 `_mark_flaw_research_landed.py`; add `tests.test_compact_land_proof` to `_lean_automation_verify_commands` required (`OVERSEER_LEAN_COMPACT_LAND_PROOF_2026_09_07`) so adapt sync cannot strip it; refresh hub-protect/vault copies + EXPECTED md5. Measure: `_land_proof` **False→True**; unittest **3/3 OK**; lean count **19** includes compact; WQ `[x]`. Metric: **queue_fp** / verify theater broken. Paths: `scripts/_mark_flaw_research_landed.py`, `scripts/automation_adapt.py`, `tests/test_compact_land_proof.py`. Distinct from L52 SCAN_BOTTLENECKS_TTL. Next: rank generation-skip / green_lock fail-closed (flaw lane).

- **2026-09-07 (peer-1 / efficiency_researcher L52)** — Completes L48–L49 Next (factory first-MISS residual ~7ms). Probe: warm factory remiss **~8.0ms** dominated by `_self_sufficiency_score` → `scan_bottlenecks` (**~6–8ms**; `_git_fingerprint` no-memo **~5ms**); generation HIT already **0.01ms**. Landed: TTL memo on default `scan_bottlenecks` (`_PROBE_TTL_SEC=30`) + `clear_scan_bottlenecks_cache`; invalidate on probe clear / apply_heals actions; `daemons=` bypass; unittest setUp clears cache. Needle `SCAN_BOTTLENECKS_TTL_2026_09_07`. Measure: scan HIT **0.003ms**; factory remiss w/ scan HIT **8.0→1.4ms** (~**5.7×**). Industry: GenerationChangedPredicate / probe TTL align. Metric: **latency**. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L44 FACTORY_PROGRESS_GENERATION / L48 LOG_TAIL / L35 PROBE_TTL. Next: rank generation-skip / noop-break Active advance.

- **2026-09-05 (peer-1 / efficiency_researcher L51)** — Probe: `team_status_for_rank` remiss paid **2×** `load_roles` (`len(load_roles())` + `worker_pool_size`→`load_roles(cfg)` bypassed memo); cProfile `_as_role` **2500**/50 miss. Landed: witness memo on default `load_roles()` + `worker_pool_size(cfg=None)`→`load_roles()`; needle `LOAD_ROLES_WITNESS_MEMO_2026_09_05`. Measure: `_as_role` **50→25**/miss; load_roles HIT **0.013ms**; for_rank remiss roles-HIT **0** `_as_role`. Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/peer_roles.py`, `tests/test_peer_roles.py`. Distinct from L41 for_rank cache / L45–L50. Pen-test ASN → `pen_tester` (asn-1788614924). Next: rank generation-skip / factory first-MISS residual.

- **2026-09-05 (peer-1 / efficiency_researcher L50)** — Assignment Break noop: Active open=0 but empty queue_fp `0000000100000000` is **truthy**, so `record_cycle_outcome` stamped **noop=True** on every verify-ok idle tick and armed backoff (oversight demoted theater; source still lied). Landed: `EMPTY_QUEUE_FINGERPRINT` + `is_empty_queue_fingerprint` exclude from noop; peer_loop log healthy-idle (not armed). Needle `HEALTHY_IDLE_EMPTY_FP_NOT_NOOP_2026_09_05`. Measure: idle empty→empty **noop False** + delivery_ts; non-empty same-fp still **noop True**. Metric: **noop broken** / queue_fp. Paths: `scripts/peer_transcript.py`, `scripts/peer_loop.py`, `tests/test_transcript.py`. 21 transcript tests OK. Distinct from L48–L49 latency/factory_pct.

- **2026-09-05 (peer-1 / efficiency_researcher L49)** — Completes L45–L47 Next (`factory_pct quick=` TypeError). Probe: `team_status` → `compute_factory_progress(quick=)` TypeError swallowed → **factory_pct=None** every gather. Landed: accept `quick=` + pass to `measure_live_state` + generation key includes quick; needle `FACTORY_PROGRESS_QUICK_KW_2026_09_05` (prefer accept over drop-kw so debrief/team keep API). Measure: TypeError gone; team_status **factory_pct=90**; MISS **32.4ms** → HIT **0.009ms**. Industry: GenerationChangedPredicate / API kw parity. Metric: **latency** + correctness. Paths: `scripts/factory_progress.py`, `tests/test_factory_progress.py`. 15 factory tests OK. Pen-test ASN → `pen_tester` (asn-1788615064). Next: residual first-MISS ~7ms after L48 LOG_TAIL.

- **2026-09-05 (peer-1 / efficiency_researcher L48)** — Completes L44 Next (first-MISS floor). Probe: hub `improve-loop.log` **20.5MB**; `_log_has_recent_any` `path.read_text()[-32k]` **37–38ms** of factory MISS **~46.8ms**. Landed: `_read_log_tail` seek-EOF + wire `_log_has_recent`/`_log_has_recent_any`; needle `LOG_TAIL_SEEK_2026_09_05`. Measure: seek **0.125ms**; factory MISS **46.8→6.8ms** (~**7×**); HIT **0.010ms**; needle parity True (byte-boundary ≠ char-slice; match unchanged). Industry: GenerationChangedPredicate / bounded log tail. Metric: **latency**. Paths: `scripts/asi_rubric.py`, `tests/test_asi_rubric.py`. 19 ASI + 15 factory tests OK. Distinct from L44 FACTORY_PROGRESS_GENERATION / L47 debrief gen. Pen-test → `pen_tester` (asn-1788615034). Next: residual ~7ms (daemon/scan).

- **2026-09-05 (peer-1 / efficiency_researcher L47)** — After L45 flaw.status_dict gen-cache, full `team_status` remiss on flaw/board still re-tailed debrief JSONL via `load_recent` (**~0.37ms** of **~0.44ms**; cold **17.8ms**). Landed: `_status_dict_input_key` (log/JSONL/count/SOP mtimes) + memo; HIT refreshes `kpis` only; `clear_status_dict_cache` on `append_entry`; needle `DEBRIEF_STATUS_DICT_GENERATION_CACHE_2026_09_05`. Measure: MISS **17.8ms** → HIT **0.01ms** (~**1780×**); `load_recent` after prime **0**. Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/peer_debrief.py`, `tests/test_peer_debrief.py`. Distinct from L45 FLAW_STATUS_DICT_GENERATION / L44 factory_progress gen / L41 for_rank. Next: factory_pct `quick=` TypeError (Factory Engineer). Pen-test ASN → `pen_tester` (asn-1788614838).

- **2026-09-05 (peer-1 / efficiency_researcher L46)** — Completes L43 Next + CREATIVE_BACKLOG "Done-open early-out". Probe: live Done opens=**0** but `_queue_md_index` still normalized every `[x]` into `orphan_done_keys` (orphans always `()`); cold med **2.734ms**. Landed: `_done_section_has_open` + gate orphan filter; needle `QUEUE_MD_INDEX_DONE_OPEN_EARLY_2026_09_05`. Measure: cold **2.734→1.503ms** (~**1.8×**); Done-open path still orphans correctly; 10 orphan tests OK. Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/project_automation.py`, `tests/test_close_landed_done_orphans.py`. Distinct from L34 single-pass / L38 phased / L43 closeable substr. Next: factory_pct `quick=` TypeError. Flaw-scan ASN → `factory_engineer` (asn-1788614982).

- **2026-09-05 (peer-1 / efficiency_researcher L45)** — Completes L43 Next (load_round parse residual). Probe: after NO_DOUBLE_READ, `status_dict` miss still **0.267ms** (56KB ROUND); team_status remiss on debrief/board mtime re-paid it though STATE+ROUND unchanged. Landed: generation key (today+past_daily+STATE/ROUND mtime) + memo; `clear_status_dict_cache` on save_*; needle `FLAW_STATUS_DICT_GENERATION_CACHE_2026_09_05`. Measure: miss **0.267ms** → HIT **0.007ms** (~**38×**); ROUND utime remiss **0.137ms**; identity HIT True. Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/peer_flaw_scan.py`, `tests/test_peer_flaw_scan.py`. 6 flaw tests OK. Distinct from L43 NO_DOUBLE_READ / L41 for_rank cache / L44 factory_progress gen. Next: factory_pct quick= fix or first-MISS floor. Pen-test ASN → `pen_tester` (asn-1788614806).

- **2026-09-05 (peer-1 / efficiency_researcher L44)** — write_team remiss + dual re-paid `compute_factory_progress` **~46–67ms**/call. Landed: generation key `(state,WQ,registry) mtime_ns` + memo; needle `FACTORY_PROGRESS_GENERATION_2026_09_05`. Measure: MISS **67.5ms** → HIT **0.010ms** (~**6700×**). Metric: **latency**. Paths: `scripts/factory_progress.py`, `tests/test_factory_progress.py`. 14 tests OK. Pen-test → `pen_tester` (asn-1788614475). Next: first-MISS floor.

- **2026-09-05 (peer-1 / efficiency_researcher L43)** — After L37 closeable gate, `_md_has_closeable_open_line` still used MULTILINE regex on ~50KB WQ (**~0.185ms**/call). Landed: substring `"- [ ]"` / `"- **["` (align L36 `close_landed`); needle `CLOSEABLE_OPEN_SUBSTRING_2026_09_05`. Measure: gate **0.185→0.015ms** (~**12×**); gather remiss **~0.52→0.21ms**; open_work **0.037ms**. Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/project_automation.py`, `tests/test_close_landed_done_orphans.py`. 9 orphan tests OK. Distinct from L36/L37 close skip / L41–L42. Next: Done-open early-out inside `_queue_md_index`. Pen-test ASN → `pen_tester` (out of scope).

- **2026-09-05 (peer-1 / efficiency_researcher L43)** — Completes L39/L40 Next (`flaw.status_dict` residual) after L41 for_rank cache + L42 loop_work. Probe: `status_dict` **0.218ms** with **4** `_safe_read_json` (state+round then nested `should_dispatch_flaw_scan` re-loaded both). Landed: `_should_dispatch_loaded(cfg,state,rnd)` + wire `status_dict` / `should_dispatch_flaw_scan`; needle `FLAW_STATUS_DICT_NO_DOUBLE_READ_2026_09_05`. Measure: reads **4→2**; status_dict **0.218→0.111ms** (~**2×**); for_rank cold **0.48→0.361ms**. Industry: single-pass load. Metric: **latency**. Paths: `scripts/peer_flaw_scan.py`, `tests/test_peer_flaw_scan.py`. Distinct from L41 RANK_TEAM_STATUS_CACHE / L42 CREATIVE_DEFERRED_INLINE. Next: load_round parse residual (~0.1ms). Pen-test ASN → `pen_tester` (asn-1788614686).

- **2026-09-05 (peer-1 / efficiency_researcher L42)** — Completes L41 Next (loop_work fp import). Probe: open=0 cold `loop_work_items` imported `factory_progress` via `_creative_is_deferred` (**8.33ms**, `fp_loaded=True`; creative=3 all covered by inline+discover). Landed: discover-first + inline-only deferred (parity with `fp._DEFERRED_MARKERS`); needle `CREATIVE_DEFERRED_INLINE_ONLY_2026_09_05`. Measure: loop_work **8.33→3.47ms** (~**2.4×**); `fp_loaded=False`; gather cold **~6.2ms**. Industry: GenerationChangedPredicate / OVERSEER_CREATIVE_DEFERRED_INLINE. Metric: **latency**/wake. Paths: `scripts/project_automation.py`, `tests/test_automation.py`. Distinct from L38–L41 team_status/phased. Pen-test ASN → `pen_tester` (asn-1788614721).

- **2026-09-05 (peer-1 / efficiency_researcher L41)** — Completes CREATIVE_BACKLOG "Cache team_status_for_rank generation" after L39 lite + L40 full-status cache. Probe: `rank_team` → uncached `team_status_for_rank` still **~0.43ms** every gather MISS though flaw/board mtimes unchanged. Landed: `_TEAM_STATUS_RANK_CACHE` + shared `_team_status_input_key` (incl. `DEBRIEF_COUNT_PATH`); needle `RANK_TEAM_STATUS_CACHE_2026_09_05`. Measure: for_rank HIT **0.022ms** (~**20×** vs warm remiss **0.43ms**); ROUND utime remisses; 8 team tests OK. Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/automation_team.py`, `tests/test_automation_team.py`. Distinct from L39 RANK_TEAM_STATUS_LITE / L40 TEAM_STATUS_GENERATION_CACHE / L38 phased. Next: Done-open early-out / loop_work fp import. Pen-test → `pen_tester` (asn-1788614679).

- **2026-09-05 (peer-1 / efficiency_researcher L40)** — Foundation for L39 lite. Probe: cold gather **5.1ms**; full `team_status` **0.915ms** every WQ-mtime gather MISS. Landed: `_team_status_input_key` (flaw/debrief/board/roles mtimes + today) + generation cache; needle `TEAM_STATUS_GENERATION_CACHE_2026_09_05`. Measure: MISS **0.915ms** → HIT **0.019ms** (~**48×**). Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/automation_team.py`, `tests/test_automation_team.py`. Distinct from L39 RANK_TEAM_STATUS_LITE / L38 phased index. Next: flaw.status_dict residual.

- **2026-09-05 (peer-1 / efficiency_researcher L39)** — Completes L34–L38 Next (rank residual). Probe: `rank_team` via full `team_status` **0.97ms** vs no-team **0.03ms**; `load_recent` **0.42ms** unused (rank only reads `deb.entries`); `factory_pct` always None (`quick=` TypeError). Landed: `team_status_for_rank` + wire `rank_team_opportunities` (needle `RANK_TEAM_STATUS_LITE_2026_09_05`); full `team_status` keeps generation cache for prompts. Measure: rank_team **0.43ms** (~**2.3×**); status_dict/load_recent on rank **0**; gather cold med **~0.85ms**. Industry: GenerationChangedPredicate / L26 board slice. Metric: **latency**. Paths: `scripts/automation_team.py`, `tests/test_automation_team.py`. Distinct from L38 QUEUE_MD_INDEX_PHASED / L37 close skip / team_status gen-cache. Next: flaw.status_dict residual (~0.21ms) or factory_pct quick= fix. Pen-test ASN → `pen_tester` (asn-1788614598).

- **2026-09-05 (peer-1 / efficiency_researcher L38)** — After L34 index (known+orphan), `sync_queue_drift` still walked `_parse_phased_work_items` separately. Landed: `phased_items` on `_QueueMdIndex` + wire parse/sync; close identity when closed=0; needle `QUEUE_MD_INDEX_PHASED_2026_09_05`. Measure: sync after parse HIT **0.017ms** (cache=1); synthetic 40-active cold sync **0.505ms**; phased test OK. Metric: **latency**. Paths: `scripts/project_automation.py`, `tests/test_close_landed_done_orphans.py`. Distinct from L34 known/orphan / L36–L37 close skip. Next: cold gather rank residual. Pen-test ASN → `pen_tester` (asn-1788614322).

- **2026-09-05 (peer-1 / efficiency_researcher L37)** — After L36 close early-exit (~0.015ms), `open_work_items` still *called* close+promote every gather. Landed: `_md_has_closeable_open_line` + skip both when no `- [ ]` / `- **[`; compact still heals. Measure: close **not called** on open=0; open_work med **~0.20ms**; gather warm **~1.37ms**. Industry: GenerationChangedPredicate / L33–L36. Metric: **latency**. Paths: `scripts/project_automation.py`, `tests/test_close_landed_done_orphans.py`. Distinct from L36 close early-exit / L34 index. Next: rank residual. Pen-test → `pen_tester`.

- **2026-09-05 (peer-1 / efficiency_researcher L36)** — After L33 compact open-marker + L34 index, `close_landed_done_orphans` still full-scanned all-[x] WQ on every `open_work_items` (probe **0.16ms**, proof calls **0**; live Active all `[x]`). Landed: substring gate `"- [ ]"` / `"- **["` → identity return; needle `CLOSE_LANDED_EARLY_EXIT_NO_OPEN_2026_09_05`. Measure: live close **0.16→0.015ms** (~**11×**); `is` identity True. Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/project_automation.py`, `tests/test_close_landed_done_orphans.py`. Distinct from L33 open-marker compact / L34 queue index. Next: cold gather rank residual. Pen-test ASN → `pen_tester` (asn-1788614409).

- **2026-09-05 (peer-1 / efficiency_researcher L35)** — After L14/L16 diagnose TTL, registry remiss still re-shelled systemd via `_PROBE_TTL_SEC=2.0` (ASI already 30s). Probe: diag cold+probe **55.9ms** / warm **10.3ms** / aged>2s **18.9ms**. Landed: `_PROBE_TTL_SEC=30.0` + needle `SELF_HEAL_PROBE_TTL_ALIGN_2026_09_05` (invalidate-on-heal unchanged). Measure: aged>2s **18.9→9.8ms** (~warm); direct probe calls after 2.1s age **0**. Industry: GenerationChangedPredicate / ASI L25. Metric: **latency**/wake. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. 37 heal+diagnose tests OK. Distinct from L14/L16 diagnose TTL / L25 ASI daemon TTL. Next: cold gather rank residual (~5–15ms).

- **2026-09-05 (peer-1 / efficiency_researcher L34)** — Completes L28–L33 Next (single-pass orphan+known). Probe: cold gather **5.08ms** CacheInfo(hits=262,misses=463); `open_done_orphan` **1.35ms** + `queue_known_keys` **1.75ms** double-scanned work md. Landed: `_queue_md_index` + `queue_md_known_keys` (hash memo) + wire orphan/known + gather→rank pass md; needle `QUEUE_MD_INDEX_SINGLE_PASS_2026_09_05`. Measure: known-after-orphan **0.002ms**; drift+known after-prime **0.140ms** (~**23×** vs cold **3.28ms**). Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/project_automation.py`, `scripts/automation_improve.py`, `tests/test_close_landed_done_orphans.py`. Distinct from L28 normalize LRU / L33 compact open-marker. Next: rank/other residual on cold gather (~5ms).

- **2026-09-05 (peer-1 / efficiency_researcher L33)** — Compact-to-12 already open=0, but `compact_executable_queue` still called `daemon_status_snapshot` (~**22ms** of **~39ms**) because `[x]` history kept `any(m in md)` True. Landed: `_md_has_open_marker` + gate snap/wake on **open** `- [ ]` only. Measure: compact dry **39→1.7ms** (~**23×**); snap/wake **0** at open=0. Metric: **latency**/queue. Paths: `scripts/project_automation.py`, `tests/test_automation.py`. 4 compact tests OK. Distinct from L32 write_team generation / L5 compact TTL / L28 normalize LRU. Next: single-pass orphan+known.

- **2026-09-05 (peer-1 / efficiency_researcher L32)** — Assignment compact→12: live `compact-queue` **open=0** (≤12; already `[x]`). Probe: wall TTL remiss on `write_team_context` still **~201ms** (learning+diagnose+factory) every 60s with unchanged WQ/state. Landed: `_team_context_input_key` (WQ+context+loop-state mtime_ns) + in-memory stamp — **generation-only** skip (wall ignored after stamp; cold process keeps 60s TTL). Measure: age>TTL HIT **0.019ms** (~**10600×** vs MISS **201ms**). Industry: GenerationChangedPredicate / prepare L24. Metric: **latency**/pre-dispatch + **queue_fp** (cap held). Paths: `scripts/peer_team_context.py`, `tests/test_peer_team_context.py`. 11 team-context tests OK. Distinct from L23–L31 gather/write_prompts/prepare generation. Next: single-pass orphan+known / registry in team-context key.

- **2026-09-05 (peer-1 / efficiency_researcher L31)** — After L23 wall TTL + L24/L28/L30 generation pattern, gather still remissed on wall age with **same** pair+flags (probe **2.61–18ms**). Landed: `_gather_cache_get` **generation-only** (key match; wall ignored; ttl<=0 miss); also cemented write_prompts wall-expire tests. Measure: wall remiss **2.61ms → 0.006ms** HIT (~**435×**). Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/automation_improve.py`, `tests/test_automation_improve.py`. 11 ImproveWriteTests OK. Distinct from L23 gather wall TTL / L24 prepare generation / L29–L30 write fp / L28 normalize. Next: single-pass orphan+known. Pen-test ASN → `pen_tester`.

- **2026-09-05 (peer-1 / efficiency_researcher L30)** — Completes L29 (tests-only omit still remissed on dirty-count). Probe: `live.git` / `footprint` / `import_rss_mb` alone remissed fp while `tests_ok`+`git_clean` stable (dirty tree **79** paths churn every edit). Landed: `_LIVE_PROMPT_FP_KEYS` allowlist (`git_clean`,`tests_ok`,`queue_source`,`open_items`) in `_live_for_prompt_fingerprint`. Measure: volatile remiss **False**; cold write **3.58ms** → volatile HIT **0.049ms** (~**73×**); `git_clean` flip still remisses. Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/automation_improve.py`, `tests/test_automation_improve.py`. 3 fp/write tests OK. Distinct from L29 tests-omit / L28 normalize LRU / L23 gather TTL. Next: single-pass orphan+known. Pen-test ASN → `pen_tester` (asn-1788614117).

- **2026-09-05 (peer-1 / efficiency_researcher L29)** — Completes L25–L27 Next (write_prompts fp rebuild on gather HIT). Probe: two cold gathers 1s apart → `fp` False solely from `live.tests` wall-clock (`0.141s`→`0.076s`; `tests_ok` True). Landed: `_live_for_prompt_fingerprint` omits `tests` (keeps `tests_ok`); needle `WRITE_PROMPTS_FP_STABLE_LIVE_2026_09_05`. Measure: fp_same True; write miss **3.4ms** → HIT **0.055ms** (~**62×**). Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/automation_improve.py`, `tests/test_automation_improve.py`. 3 write/fp tests OK. Distinct from L23 gather TTL / L24 prepare / L25 daemon / L26 board / L27 emit list / L28 normalize LRU. Next: single-pass orphan+known (open). Pen-test eval() ASN → `pen_tester`.

- **2026-09-05 (peer-1 / efficiency_researcher L28)** — After L26 slice (gather lists=0) + L27 cold emit list skip, cold gather still paid `_normalize_queue_key` **~726×**/call (**~462** unique, **~264** intra-gather dups via orphan/drift/known). Probe med **6.5ms**. Landed: `@lru_cache(4096)` + `clear_normalize_queue_key_cache`. Measure: cold-clear med **5.6ms** (hits=262); warm-keys med **3.1ms** (~**2.1×**). Industry: memoized key normalize. Metric: **latency**/gather. Paths: `scripts/project_automation.py`, `tests/test_close_landed_done_orphans.py`. Distinct from L26 board slice / L27 cold emit list / L23 gather TTL. Next: single-pass orphan+known. Pen-test eval() ASN → `pen_tester` (out of scope).

- **2026-09-05 (peer-1 / efficiency_researcher L27)** — After L21 list TTL, cold `_emit_list_stamp_at=0` still shelled `list_worktrees` on first ensure-skip (pre-change probe emit **2.63ms** with list). Landed: `_should_skip_emit_list(allow_cold=True)` + stamp without porcelain when ensure floor-skipped; TTL expiry still re-lists. Measure: cold emit med **0.14ms**, `list_worktrees` **0** calls. Closed false-open WQ git_fp contextvar memo (already wired L prior; WQ↔context `[x]`). Metric: **latency**/wake + **queue_fp**. Paths: `scripts/peer_loop.py`, `tests/test_after_verify_ensure_ttl.py`, `notes/WORK_QUEUE.md`. 9 ensure/list tests OK. Distinct from L20/L21 ensure/list TTL / L24 prepare generation / L26 board slice. Next: write_prompts fp rebuild on gather HIT (~27ms).

- **2026-09-05 (peer-1 / efficiency_researcher L26)** — Completes L25 Next (`team_status` cold). Probe: `team_status` → `build_board` → `list_worktrees` **~2.5–3.0ms** though only `summary`+`dispatch_iso` kept (`automation_team.py:66`); slice **0.08ms**. Landed: `agents_board_slice()` (runtime JSON + roster; no porcelain) + `team_status` wire; `agents_board_lite` alias. Measure: board-path **2.96ms** → slice **0.08ms** (~**37×**); gather cold **~13ms** (rank/debrief remain). Industry: GenerationChangedPredicate. Metric: **latency**/gather. Paths: `scripts/peer_agent_board.py`, `scripts/automation_team.py`, `tests/test_peer_agent_board.py`, `tests/test_automation_team.py`. 9 board/team tests OK. Distinct from L23 gather TTL / L24 prepare generation / L25 daemon probe. Next: write_prompts fp rebuild on gather HIT (~27ms).

- **2026-09-05 (peer-1 / efficiency_researcher L25)** — After L23 gather HIT + L24 prepare generation-skip, write_prompts/ASI still re-shelled daemon probes: probe **2×** `compute_asi_progress` = **21.0ms** (`systemd=4` × ~**5ms**; Linux tried `launchctl` first). Landed: `DAEMON_PROBE_TTL_SEC=30` + `_DAEMON_PROBE_CACHE` on `_launchctl_running`; Linux skips `launchctl` → systemd only; `clear_daemon_probe_cache`. Measure: miss **~5ms**/label → HIT **<0.05ms**; 2× progress second pass **0** systemd. Industry: GenerationChangedPredicate. Metric: **latency**/ASI. Paths: `scripts/asi_rubric.py`, `tests/test_asi_rubric.py`. Distinct from L23 gather TTL / L24 prepare generation / write_prompts fp TTL. Next: write_prompts fp rebuild on gather HIT (~27ms) / `team_status` cold ~33ms.

- **2026-09-05 (peer-1 / efficiency_researcher L24)** — After L22 wake-floor (eff=35), prepare still remissed when `age>eff` with **same** queue-pair mtimes — probe skip@age>eff **False**; gather still **~18–33ms**; write_prompts already **0.02ms**. Landed: `should_skip_prepare_continuous_prompts` **generation-only** (pair match; wall age ignored; `now` API kept). Measure: age>eff HIT **True**; prepare **0.026ms** (~**1200×** vs MISS **33ms**). Industry: [GenerationChangedPredicate](https://github.com/kubernetes-sigs/controller-runtime/blob/main/pkg/predicate/predicate.go) · Turborepo fingerprint · SQS long-poll residual. Metric: **latency**/wake. Paths: `scripts/peer_loop.py`, `tests/test_automation.py`. 2 prepare tests OK. Distinct from L22 wake-floor maint / L23 gather input-hash TTL / L9 prepare stamp. Next: write_prompts fp rebuild on gather HIT (~27ms).

- **2026-09-05 (peer-1 / efficiency_researcher L23)** — Completes L22 Next (true prepare miss / post-wake remiss still paid gather). Probe: gather audit=False cold **22ms** / HIT **0.006ms**; rank dominates uncached. Landed: `_queue_pair_mtime_ns` + `_gather_input_key` + `gather_signals_ttl_sec` (default **120**, CFG) + in-memory cache; bypass `live_state` / `refresh_trends`. Measure: cold **22→0.006ms** (~**3600×**); flag change remiss **25ms**. Industry: GenerationChangedPredicate. Metric: **latency**. Paths: `scripts/automation_improve.py`, `tests/test_automation_improve.py`. 2 gather-TTL + ImproveWriteTests OK. Distinct from L22 wake-floor maint TTL / L9 prepare stamp. Next: write_prompts rebuild when gather HIT but fp stale.

- **2026-09-05 (peer-1 / efficiency_researcher L22)** — L21 Next (gather on prepare miss) root cause was wake-floor: local `continuous_wake_sec=30` == `PRE_DISPATCH_MAINT_TTL_SEC=30` → prepare/compact/sync remiss every continuous wake (`age=wake >= TTL`). Probe: skip@age=30 **False**; gather_auditFalse **~10–18ms**; prepare miss **~67ms**. Landed: `pre_dispatch_maint_effective_ttl_sec()` = max(PRE_TTL, wake+slack5) → **35**; wired prepare/compact/sync. Measure: age=wake HIT **True**; prepare age=30 **0.02ms** (~**3000×**). Industry: GenerationChangedPredicate / find_latest L7 wake-slack. Metric: **latency**/wake. Paths: `scripts/peer_loop.py`, `tests/test_automation.py`. 3 maint tests OK. Distinct from L9 prepare stamp / L15 sync TTL / L21 emit list. Next: true queue-mtime prepare miss still pays gather rank (~17ms).

- **2026-09-05 (peer-1 / efficiency_researcher L21)** — After L20 cold ensure-skip, `_emit_worktree_inventory` still shelled `git worktree list --porcelain` every continuous wake ("list only"; probe list **~2.0ms**, emit **3.4ms**×2). Landed: `_emit_list_stamp_at` + `_should_skip_emit_list` (same `EMIT_ENSURE_TTL_SEC`); skip inventory when ensure skipped + stamp fresh; clear with `clear_emit_ensure_cache`. Measure: real emit **2.56→0.18ms** (~**14×**). Industry: GenerationChangedPredicate. Metric: **latency**/wake. Paths: `scripts/peer_loop.py`, `tests/test_after_verify_ensure_ttl.py`. 8 ensure/list tests OK. Distinct from L20 cold floor ensure / L18 isdir pool / L6 warm ensure TTL. Next: gather_signals cold ~20ms on prepare miss.

- **2026-09-05 (peer-1 / efficiency_researcher L20)** — After L6 warm TTL, cold `_emit_ensure_at=0` still forced `ensure_parallel_pool` (probe **63–75ms**; floor `is_dir` **0.255ms**; emit#0 **165.9ms**). Landed: `_should_skip_emit_ensure` skip when floor ready even cold; `_stamp_emit_ensure_on_cold_skip` so TTL still re-prunes; wired emit + `_after_verify_ok`. Measure: cold emit **12.8ms** (~**13×**; recheck **8.2ms**); TTL-expired still ensures. Industry: [GenerationChangedPredicate](https://github.com/kubernetes-sigs/controller-runtime/blob/main/pkg/predicate/predicate.go). Metric: **latency**/wake. Paths: `scripts/peer_loop.py`, `tests/test_after_verify_ensure_ttl.py`. 6 ensure-TTL tests OK. Distinct from L6 warm TTL / L18 isdir pool / L19 soft-warn skip / L17 sticky TTL.

- **2026-09-05 (peer-1 / efficiency_researcher L19)** — Complements L17 adapt early-soften. Probe: Episodic-only `verify_ok=false` → playbook heal-all **5624ms** before soften. Landed pre-dispatch short-circuit: `fail_rows_only_soft_reheal_skippable` + Episodic `warn:`→status warn; `_mechanical_pre_dispatch` softens **without** `adapt_plan_gate` (skips sync-refresh + heal-all). Soft soften **0.00ms**; drift still adapt. Metric: **latency**. Paths: `scripts/peer_agent_gates.py`, `scripts/peer_loop.py`, `tests/test_peer_agent_gates.py`. 10 gate tests OK. Distinct from L17 sticky TTL inside adapt / L18 isdir pool / L15 sync-queue.

- **2026-09-05 (peer-1 / efficiency_researcher L18)** — After L14–L17, plan-gate still shelled `git worktree list --porcelain` via `_worktree_pool_ok` → `list_worktrees` every dispatch (probe **3.2ms**). Floor only needs hub `peer-0..N-1` exist — emit TTL already uses is_dir. Landed is_dir floor (`WORKTREE_POOL_OK_ISDIR`); measure **0.19ms** (~**17×**); plan_gate no longer lists porcelain. Industry: GenerationChangedPredicate / Turborepo#12106. Metric: **latency**/wake. Paths: `scripts/peer_agent_gates.py`, `tests/test_peer_agent_gates.py`. Distinct from L17 soft chicken-egg / L16 diagnose mtime-key / L15 sync-queue / prior in-process list (still porcelain). Next: `_secret_scan_staged` git diff --cached every plan-gate (~3ms) — index mtime TTL.

- **2026-09-05 (peer-1 / efficiency_researcher L17)** — Completes L8/L15 Next. Probe: `adapt_plan_gate` Self-correction fail `"Peer loop daemon not running"` → playbook `obs_1d9720c165` → `heal-all` **1539.9ms**. Landed: `is_soft_chicken_egg_only` early-soften + sticky TTL (`SOFT_WARN_TTL_SEC=30`, in-memory + `error-adapt-soft-warn.json`); recoverable softens also stamp; gates add `daemon_peer_stopped` soft needle. Measure: first **0.02ms** / TTL **0.01ms**; no `cmd:heal-all`. Metric: **latency**. Paths: `scripts/peer_error_adapt.py`, `scripts/peer_agent_gates.py`, `tests/test_peer_error_adapt.py`. 10 error-adapt tests OK. Distinct from L16 diagnose mtime-key. Next: playbook obs demote vs canonical `daemon_peer_stopped` (install/status).

- **2026-09-05 (peer-1 / efficiency_researcher L16)** — L14 wall-clock diagnose TTL could serve stale findings for **30s** after heal/cycle writes. Probe: miss **47ms** / HIT **0.027ms**; registry `utime` within TTL → remiss **10.4ms** (scans=2). Landed `_diagnose_cache_key` (registry + peer-loop-state `mtime_ns`) on `_QUICK_DIAG_CACHE`. Metric: **latency** + freshness. Paths: `scripts/peer_self_diagnose.py`, `tests/test_peer_self_diagnose.py`. 5 diagnose tests OK. Distinct from L14 wall-clock TTL / L15 sync-queue skip.

- **2026-09-05 (peer-1 / efficiency_researcher L15)** — Memory cited `should_skip_pre_dispatch_sync_queue` but source always ran `heal_queue_drift` in `_mechanical_pre_dispatch` (probe **3.1–6.9ms**; HIT pre **~23.6ms** with compact+self_heal already skipping). Landed: stamp after empty heal + pair-mtime TTL (`PRE_DISPATCH_MAINT_TTL_SEC`); clear on heal/warns or compact remove. Measure: HIT pre **23.6→12.5ms**; log `sync-queue skip (TTL/mtime)`. Industry: [GenerationChangedPredicate](https://github.com/kubernetes-sigs/controller-runtime/blob/main/pkg/predicate/predicate.go). Metric: **latency**. Paths: `scripts/peer_loop.py`, `tests/test_automation.py`. Distinct from L5 compact/self_heal / L9 prepare / L13 post-wait / L14 diagnose TTL. Next: plan-gate soft-fail re-heal (~1.8s).

- **2026-09-05 (peer-1 / efficiency_researcher L13)** — Even after L12 force-keep-primary (~**1.5ms**/force), Darwin post-wait still called `force=True` after `PeerEventWatcher.wait` already force-scanned (`force=bool(events)`). Probe (legacy full): wait **203.8ms** + post **190.8ms** vs soft **0.030ms**; under keep-primary soft still avoids a second primary scan. Landed `POST_WAIT_FIND_LATEST_FORCE=False` + needle `REUSE_WAIT_FIND_LATEST_CACHE`. Metric: **latency**/wake. Paths: `scripts/peer_loop.py`, `tests/test_automation.py`. Distinct from L4 fallback-skip / L12 force-keep-primary / L12 diagnose TTL.

- **2026-09-05 (peer-1 / efficiency_researcher L12)** — After L10 scandir, `force=True` still flipped `_resolve_find_latest_primary_only` → watch_all (~**212ms**; pre-scandir **640ms**) and could return another peer's transcript. Probe: force+primary **2.9ms**. Industry: MapFunc / turbo watch locality. Landed: force bypasses TTL only; CFG primary_only (default True) stays on (`FIND_LATEST_FORCE_KEEP_PRIMARY`). Measure: force med **1.5ms** (~**140×**). Paths: `scripts/peer_transcript.py`, `tests/test_transcript.py`. 20 transcript tests OK. Metric: `latency`. Distinct from L8/L10/L11 wake-floor.

- **2026-09-05 (peer-1 / efficiency_researcher L14)** — After L9–L11, plan-gate still re-paid `run_instant_diagnosis` → `scan_bottlenecks` every pre-dispatch (cProfile ~**25ms**/call; cold quick **18.7ms**; plan_gate warm was ~**60ms**). Landed `plan_gate_diagnose_ttl_sec` (default **30**) + `_QUICK_DIAG_CACHE` on quick `run_instant_diagnosis`; CLI `--scan` uses `force=True`. Measure: TTL hit **0.001ms** (~**18700×**); plan_gate warm **13.1ms**. Metric: `latency`/`wake`. Paths: `scripts/peer_self_diagnose.py`, `tests/test_peer_self_diagnose.py`. Distinct from L5/L9 pre-dispatch maint / L10 scandir / L12 force-keep-primary / L13 post-wait reuse.

- **2026-09-05 (peer-1 / efficiency_researcher L11)** — After L8 primary_only (~**2ms** soft) + L10 scandir force (~**196ms**), wake-floor still forced effective TTL **35** → Linux loop-head stale ≤35s for no win. Landed: `_find_latest_ttl_sec` skips wake-floor when soft primary_only; legacy full soft still floors. Probe: TTL **3.0**; soft miss age=3.5 **1.73ms**; warm **0.005ms**; legacy floor **35** when primary_only=False. Metric: **latency**/freshness. Paths: `scripts/peer_transcript.py`, `tests/test_transcript.py`. 20 transcript tests OK. Distinct from L8/L10.

- **2026-09-05 (peer-1 / efficiency_researcher L10)** — Completes L8 force tax. Probe: Path.iterdir+double-stat force med **623.9ms**/~38k; `os.scandir`+stat one-pass med **181.9–220ms** (PEP 471). Landed `_iter_parent_transcript_candidates` + `_newest_parent_transcript_path`; `find_latest_transcript` one-pass (no list+max restat). Metric: **latency** (~**400ms**/VNODE force). Paths: `scripts/peer_transcript.py`, `tests/test_transcript.py`. 20 transcript tests OK. Next: plan-gate soft-fail re-heal (~1.8s). Distinct from L8 primary_only / L9 prepare TTL.

- **2026-09-05 (peer-1 / efficiency_researcher L9)** — After write_prompts TTL, `_prepare_continuous_prompts` still paid `gather_signals` every continuous wake (probe **p1=72.4ms** / **p2=9.9ms**; write alone **0.2ms**). Landed `should_skip_prepare_continuous_prompts` (queue-pair stamp + `PRE_DISPATCH_MAINT_TTL_SEC`) — **p2=0.0ms** + `prompts skip (queue pair TTL)`. Metric: `latency`/`wake`. Paths: `scripts/peer_loop.py`, `tests/test_automation.py`. Distinct from L5 pre-dispatch compact/self_heal TTL / L8 primary_only / write_prompts ASI TTL.

- **2026-09-05 (peer-1 / efficiency_researcher L8)** — Completes L4 Next. Probe: primary **561** paths **6.5ms** vs watch_all **38935** / **457ms** list; non-force cold find_latest **8.5ms** vs force **640.7ms**. Landed `primary_transcript_root` + `_parent_transcript_paths(primary_only=)` + CFG `transcript_find_latest_primary_only` (default True) — non-force/loop-head primary-only; `force=True` keeps watch_all for VNODE freshness. Metric: **latency** (~**630ms**/TTL-miss loop-head). Paths: `scripts/peer_transcript.py`, `scripts/automation_config.py`, `automation.config.json`, `tests/test_transcript.py`. 18 transcript tests OK. Next: plan-gate soft-fail re-heal tax (~1.8s/warm dispatch).

- **2026-09-05 (peer-1 / efficiency_researcher L7)** — `age < ttl` with TTL==wake still MISSed timeout loop-head (probe miss **627ms**, age=wake **637ms**, age=wake−0.5 HIT **0.03ms**; **38887** paths). Landed `_FIND_LATEST_WAKE_SLACK_SEC=5` + CFG `transcript_find_latest_wake_slack_sec` → effective TTL **35** (wake=30); at_wake HIT **0.026ms**. Metric: **latency**/wake. Paths: `scripts/peer_transcript.py`, `tests/test_transcript.py`, `scripts/automation_config.py`. 17 transcript tests OK. Compression TRAIN_RECIPE ASN → compression_researcher; peer-loop Type=simple/--daemon → sre_release.

- **2026-09-05 (peer-1 / efficiency_researcher L6)** — False `[x]`: WQ claimed skip ensure in `_after_verify_ok` but source always called `ensure_parallel_pool` (tests/test_after_verify_ensure_ttl.py **FAIL**). Probe: cold ensure slice **52.3ms**/green tick; emit already TTL-skips. Re-landed: reuse `_should_skip_emit_ensure` + stamp `_emit_ensure_at` on miss. Measure: skip **0.3ms** (~**174×**). Industry: GenerationChangedPredicate · worktrunk#2259. Metric: `latency`/`wake`. Paths: `scripts/peer_loop.py`, `tests/test_after_verify_ensure_ttl.py`. Distinct from emit TTL / L5 pre-dispatch maint TTL / find_latest.

- **2026-09-05 (peer-1 / efficiency_researcher L5)** — Probe: warm `_mechanical_pre_dispatch` paid compact **26.3ms** + self_heal **20.2ms** every dispatch even when no-op; memory cited `_should_skip_pre_dispatch_*` but helpers **absent**. Landed: `PRE_DISPATCH_MAINT_TTL_SEC=30` + `should_skip_pre_dispatch_compact` (stamp after removed=0; clear on remove; mtime pair match) + `should_skip_pre_dispatch_self_heal` (REGISTRY_PATH age < TTL); wired into `_mechanical_pre_dispatch`. Metric: `latency` (~**46ms**/warm dispatch). Paths: `scripts/peer_loop.py`, `tests/test_automation.py`. Distinct from L4 Linux fallback skip / write_team TTL / find_latest TTL. Soft: done-gate Self-correction now mirrors plan-gate soft list (daemon inactive on Loop:exhausted).

- **2026-09-05 (peer-1 / efficiency_researcher L4)** — Linux DGX: `watcher.available=False` → post-wait always `reason=fallback` + prior `force=True` **bypassed TTL floor**, double-paying watch_all (~**610ms** miss / **0.03ms** hit; **38871** paths). Darwin timeout-skip alone left Linux cold. Landed `event_reason=` on `should_skip_post_wait_transcript_rescan` / live_refresh — skip **fallback** when `!watcher_available` (reuse loop-head); transcript/signal/git still force rescan. Materialized `transcript_find_latest_ttl_sec: 3.0` in `automation.config.json`. Metric: **latency** (~610ms/Linux sleep wake). Paths: `scripts/peer_loop.py`, `tests/test_automation.py`, `automation.config.json`. Next: primary-only loop-head (~**7.7ms**/550 vs ~**434ms** list).

- **2026-09-05 (peer-1 / efficiency_researcher L3)** — TTL=3 ≪ wake=30 left every continuous heartbeat cold (~**642ms**/38k paths) despite L2 cache. Floor `_find_latest_ttl_sec` at `continuous_wake_sec`(+slack) when watch_all; explicit **0** via `is None` (not `or 3`). Landed `PeerEventWatcher.wait` **`force=bool(events)`**. Measure: effective_ttl **35** (wake+5); age=5s/29s HIT **~0ms**; cold **~612ms**. Metric: **latency**/wake (~600ms/timeout saved). Paths: `scripts/peer_transcript.py`, `tests/test_transcript.py`.

- **2026-09-05 (peer-1 / efficiency_researcher L2)** — Completes prior note's Next (TTL inside `find_latest`). Probe: **38822** paths / **98** roots; cold force med **650ms**; slug was `…Automation-.worktrees-peer-1` → primary **missing**. Landed: (1) `_slug_path_parts` strip leading `.` — primary **exists**; (2) `transcript_find_latest_ttl_sec` default **3.0** cache on `find_latest_transcript` (`force=True` bypass) — warm **0.0ms** (~**650ms**/loop-head saved within TTL). Post-wait skip already landed L1. Metric: `latency`/`wake`. Paths: `scripts/peer_transcript.py`, `scripts/automation_config.py`, `tests/test_transcript.py`. 14 transcript tests OK.

- **2026-09-05 (peer-1 / efficiency_researcher)** — timed probe `find_latest_transcript`: **38815** paths, median **658ms**/call; forever loop paid it at loop-head **and** post-wait ≈ **1.3s**/wake. Landed `should_skip_post_wait_transcript_rescan` / `should_skip_post_wait_live_refresh` — kqueue `timeout` + watcher → reuse path/meta/live. Metric: **latency** (~658ms saved per timeout heartbeat). Paths: `scripts/peer_loop.py`, `tests/test_automation.py`.

- **2026-09-03 L2 (hub / peer-9 missing)** — hub timed probe (`env -u RAM_AUTOMATION_NO_SUBTEST PYTHONPATH=scripts`, wake=30, peers=8, open=12–26); peer-9 worktree absent (pool cap peer-0..7):
  - **`_git_common_dir` false `[x]`** — WQ claims Parse gitfile landed but hub+peer-1 still `subprocess.run(["git","rev-parse","--git-common-dir"])` (no gitdir/commondir FS read). Instrument ensure: **30×** gcd / **11×** list / **8×** coding; gcd time **44.2ms** of ensure **133ms**. Micro: hub subprocess **1.17ms**/call vs gitfile **0.014ms** (~**84×**); peer-1 **0.97→0.11ms**.
  - Confirm open (do not re-enqueue): Batch-list ensure_coding (just queued); emit ensure re-land; `_after_verify_ok` ensure skip; write_prompts ASI TTL; write_team_context TTL; stall adapt-audit re-land; same-tick stall skip. Landed: `_worktree_pool_ok` in-process (**2.1ms**); close-asi skip (**0.1ms**); self-check body **38.9ms**.
  - Continuous tax: emit **133–143ms**; write_prompts **96ms** (2nd still **97ms**); prepare **113ms**; write_team_context **91–124ms**; plan-gate refresh=True **109ms** / False **12.7ms**; stall still shells adapt `--audit` when `should_re=False`.
  - Per-worker yield: every continuous + verify_ok wake serial-steals ~**130ms** ensure dominated by gcd fork tax (~33% of ensure); at parallel=8 workers wait on same lock shape.
  - Industry: [Turborepo #12106](https://github.com/vercel/turborepo/pull/12106) replace `git rev-parse --git-common-dir` with filesystem gitdir/commondir reads.
  - Enqueued `**[efficiency-research] Re-land Parse gitfile in peer_worktree._git_common_dir** — falsely `[x]` but hub source still shells `git rev-parse --git-common-dir` (no gitdir/commondir read); ensure pays **30×** gcd (**44.2ms** of **133ms** ensure); hub **1.17ms**/call vs gitfile **0.014ms** (~**84×**); peer-1 **0.97→0.11ms**; Turborepo [#12106](https://github.com/vercel/turborepo/pull/12106); file `scripts/peer_worktree.py`; distinct from Batch-list / emit TTL / `_after_verify_ok` ensure skip; unittest; `./scripts/peer test-quick``. File `scripts/peer_worktree.py`. Distinct from Batch-list / emit TTL / `_after_verify_ok`.

- **2026-09-03 06:09:33 L2 (peer-20 missing → hub)** — hub timed probe (`env -u RAM_AUTOMATION_NO_SUBTEST PYTHONPATH=scripts`, peers=8, wake=90, stall=3):
  - **`ensure_parallel_pool` 11× `list_worktrees` hot path** — floor loop calls `ensure_coding_worktree` once per peer-0..N-1; each re-lists. Trace: **8×** `ensure_coding_worktree` + prune_nested + prune_excess + extras = **11** lists/ensure. Probed: ensure full **125–130ms**; 8× coding **33–36ms**; 1× list+match-8 **13ms**; list alone **1.5–2.3ms**; Path.is_dir×8 **1.6ms**. Align slice ~**60ms** of ensure (ensure no-align **65ms**).
  - Per-worker yield: every continuous emit + `_after_verify_ok` (still unconditional) re-burns **11** porcelain lists before dispatch; at parallel=8 workers serialize on the same ensure lock shape (~**130ms**/tick).
  - Confirm open (do not re-enqueue): emit ensure TTL re-land; `_after_verify_ok` ensure skip; stall adapt-audit re-land; write_prompts ASI TTL; write_team_context TTL (demoted); same-tick stall skip (demoted). close-asi skip **0.04ms**; plan-gate in-process list **1.1ms**.
  - Side: `write_team_context` **88–94ms** (`project_learning` write **29ms** + `factory_progress` **25ms`); `_prepare_continuous_prompts` **110–126ms** (gather~22 + write_prompts~93) — covered by open ASI/TTL items.
  - Industry: [worktrunk#2259](https://github.com/max-sixty/worktrunk/pull/2259) share git porcelain · [Turborepo #12106](https://github.com/vercel/turborepo/pull/12106) cut fork/list thrash on SCM path.
  - Enqueued `[efficiency-research] Batch-list once in ensure_parallel_pool (stop 8× ensure_coding list_worktrees)`. File `scripts/peer_worktree.py`. Distinct from emit TTL / after_verify ensure skip / gitfile gcd.
- **2026-09-03 L3 (peer-31→hub)** — hub timed probe (`PYTHONPATH=scripts`); peer-31 absent (pool cap peer-0..7):
  - **`run_local_pivot_work` adapt `--audit` false `[x]`** — claimed TTL-skip landed but hub source still shells `automation_adapt.py --audit` (90s) with no `_maybe_stall_adapt_audit`. Prior probe **7257ms** vs `detect_signals(quick)` **4.8ms** (~**1500×**). Live: `--audit` still in source.
  - **plan-gate refresh tax** — `run_plan_gate(refresh=True)` **103–109ms** vs `refresh=False` **11.5–14.9ms** (~**9×**); `_refresh_team_context` always writes. Covered partly by backlog write_team_context TTL — do not re-enqueue; note only.
  - **emit ensure false `[x]`** — confirm open (already queued): `_emit_worktree_inventory` always `ensure_parallel_pool`; probed emit **129–151ms**, list-only **1.3–2.0ms**.
  - Per-worker yield: stall_pivot_sec=3 + shell audit steals entire wake; workers wait on verify/lock shape.
  - Industry: [GenerationChangedPredicate](https://github.com/kubernetes-sigs/controller-runtime/blob/main/pkg/predicate/predicate.go) · [reconcile-loop explosions 2026](https://www.golinuxcloud.com/avoid-kubernetes-operator-reconcile-loop-explosions/) — skip expensive refresh when generation/TTL unchanged.
  - Enqueued `[efficiency-research] Re-land TTL-skip stall-pivot adapt --audit subprocess`. File `scripts/peer_stall_pivot.py`. Distinct from Same-tick / emit ensure re-land / write_team_context.
- **2026-09-03 L2 (peer-20 pruned → hub)** — hub timed probe (`PYTHONPATH=scripts`); peer-20 excess-pruned mid-cycle:
  - **`_emit_worktree_inventory` ensure still unconditional** — WQ falsely `[x]` but source always calls `ensure_parallel_pool` (no TTL/age/ready). Probed ensure **104ms**; emit **85–90ms**; immediate re-emit **~90ms**; `list_worktrees` **1.2–2.0ms** (~**50×**).
  - Per-worker yield: every continuous wake serial-steals ~**90–100ms** before keep-working; parallel=8 waits on same shape; excess prune self-evicted research lane peer-20.
  - Side: `write_team_context` **129ms**; `scan_bottlenecks` **85ms** (already covered by write_team TTL queue). `_after_verify_ok` ensure still open.
  - Enqueued **re-open** `[efficiency-research] Re-land TTL/ready-skip ensure in peer_loop._emit_worktree_inventory`. Distinct from `_after_verify_ok` ensure skip / write_team_context / write_prompts.

- **2026-09-03 L3 (cycle 56 / peer-9→hub)** — hub timed probe (`env -u RAM_AUTOMATION_NO_SUBTEST PYTHONPATH=scripts`, wake=30, peers=8); peer-9 pruned by pool cap:
  - **`write_prompts` ASI compute hot path** — string-reuse already landed (`build_combined_prompt` no longer 4×) but `write_prompts` still always calls `build_plan_prompt`+`build_execute_prompt` → `compute_asi_progress` **2×**. Probed: write **85–93ms**; ASI **33–37ms**/call (**2 calls**); `_write_text_if_changed` no-op still pays the full build. `gather_signals(research=True)` **53ms**; `write_team_context` **122ms** (already queued); `ensure_parallel_pool(8)` **88ms** (already queued via `_after_verify_ok`).
  - Per-worker yield: `_prepare_continuous_prompts` every wake pays ~**86ms** of ASI rubric with zero prompt change — serial steal after close-asi skip. Queue still **bloated** (launch source); compact-to-12 already `[x]`.
  - Confirm open (do not re-enqueue): skip ensure in `_after_verify_ok`; write_team_context TTL; same-tick stall skip; emit TTL claimed `[x]` but ensure still **88ms**.
  - Industry: [GenerationChangedPredicate](https://github.com/kubernetes-sigs/controller-runtime/blob/main/pkg/predicate/predicate.go) skip when generation unchanged · [pytest-delta](https://pypi.org/project/pytest-delta/) don't rebuild when inputs unchanged · [ECOA idempotency](https://ecoaai.com/multi-agent-idempotency-layer-production-fix/) don't re-run measure on timeout/retry · [worktrunk#2259](https://github.com/max-sixty/worktrunk/pull/2259) share git porcelain · `git --no-optional-locks` on remaining `_git_fingerprint`/`_measure_git` shells.
  - Enqueued `[efficiency-research] Skip write_prompts ASI compute when bytes/TTL fresh`. File `scripts/automation_improve.py`. Distinct from string-reuse (landed) / emit TTL / write_team_context.
- **2026-09-03 L2 (cycle 56 / peer-21→hub)** — hub timed probe (`env -u RAM_AUTOMATION_NO_SUBTEST PYTHONPATH=scripts`, wake=30, peers=8, stall_pivot_sec=3); peer-21 pruned by pool cap:
  - **`_after_verify_ok` ensure hot path** — every green tick (incl. continuous verify_ok) re-calls `ensure_parallel_pool` after emit already paid it. Probed: ensure alone **88–99ms**; `_after_verify` ensure slice **95–101ms**; same-wake double-tax ~**190ms**. close-asi skip **0.08ms** (landed); write_prompts still **91–96ms** (2× `compute_asi_progress` **37–40ms**); write_team_context **89–95ms**.
  - Per-worker yield: at wake=30, ~**95ms**/green continuous tick is pure serial steal from dispatch even after emit TTL lands; cooldown-skip theater still arms verify_ok → ensure (flaw already queued).
  - Confirm open (do not re-enqueue): plan-gate in-process list_worktrees (L3 cycle 56); write_team_context TTL; write_prompts reuse; same-tick stall skip; emit TTL claimed `[x]` but hub emit still always ensures (**103ms**); stall adapt-audit `[x]`.
  - Industry: [GenerationChangedPredicate](https://github.com/kubernetes-sigs/controller-runtime/blob/main/pkg/predicate/predicate.go) · [Turborepo #12106](https://github.com/vercel/turborepo/pull/12106) · [CPython #146181](https://github.com/python/cpython/issues/146181) fork+execve — skip refresh when pool ready/generation unchanged.
  - Enqueued `[efficiency-research] Skip ensure_parallel_pool in peer_loop._after_verify_ok when pool ready`. File `scripts/peer_loop.py`. Distinct from emit TTL / write_team_context / write_prompts / plan-gate list_worktrees.
- **2026-09-03 L3 (cycle 56 / hub)** — hub timed probe (`env -u RAM_AUTOMATION_NO_SUBTEST PYTHONPATH=scripts`, wake=30, peers=8, open=10):
  - **`peer_agent_gates._worktree_pool_ok` pre-dispatch hot path** — `run_plan_gate(refresh=False, quick=True)` shells `python3 scripts/peer_worktree.py list` every dispatch. Probed: subprocess list **50.7–66.3ms**; `_worktree_pool_ok` **66.8–78.0ms**; in-process `list_worktrees` **1.3–1.9ms** (**~35–40×**); plan-gate total **68.4–88.5ms** (fork dominates). `write_team_context` **90.8–129.7ms**; pre_dispatch sim **185.2ms**.
  - Per-worker yield: after write_team_context TTL lands, this fork is the remaining serial steal before cursor-agent start; at parallel=8 workers wait on the same lock shape.
  - Confirm open (do not re-enqueue): write_team_context TTL; write_prompts reuse (still **87–95ms** compute); same-tick stall skip; emit ensure TTL (`[x]` queue / hub emit still **96ms** first tick); stall adapt-audit `[x]`; close-asi skip **0.1ms**.
  - Industry: [Turborepo #12106](https://github.com/vercel/turborepo/pull/12106) (fork+git startup on SCM path) · [GenerationChangedPredicate](https://github.com/kubernetes-sigs/controller-runtime/blob/main/pkg/predicate/predicate.go) · [GHA lookup-only cache](https://github.com/actions/cache/pull/1041).
  - Enqueued `[efficiency-research] In-process list_worktrees in peer_agent_gates._worktree_pool_ok`. File `scripts/peer_agent_gates.py`. Distinct from emit TTL / write_team_context / write_prompts / stall adapt-audit.
- **2026-09-03 (cycle 55 / peer-1)** — `run_local_pivot_work` shelled `automation_adapt.py --audit` (no `--quick`, 90s timeout) every stall when `should_re_adapt` is False. Live: `should_re=False`; audit age **15927s**; `detect_signals(quick)` **3.57ms**. Emit TTL-skip ensure already landed c53. Fix: `_maybe_stall_adapt_audit` — skip unless adapt-audit.json stale or in-process `ADAPT_AUDIT_TTL_SEC=60`; miss → `detect_signals(quick)`, never subprocess. Measure: miss **7.75ms** / TTL hit **3.43ms** vs probe **7257ms** (~**935–2100×**); 10 stall-pivot tests OK. Metric: `latency`. Path: `scripts/peer_stall_pivot.py`. Next: `write_prompts` 4× ASI.
- **2026-09-03 L2 (cycle 55 / peer-21→hub)** — hub timed probe (`env -u RAM_AUTOMATION_NO_SUBTEST PYTHONPATH=scripts`, wake=30, peers=8, stall_pivot_sec=3):
  - **`run_local_pivot_work` kit re-entry hot path** — always re-calls `peer_loop._emit_worktree_inventory` + `_close_completed_asi_phase_plans` + `_prepare_continuous_prompts` with no same-tick gate after continuous already paid them. Probed: emit **103ms** → immediate re-emit **91ms**; close **165→71ms**; prepare **113→113ms**; full stall no-audit (should_re_adapt forced True) **396ms** (ensure alone **93ms**).
  - Per-worker yield: stall_pivot_sec=3 means a blocked wake re-burns ~**400ms** of keep-working that continuous already spent — serial steal from dispatch; Cursor self-driving agents wait on the same lock shape.
  - Confirm open (do not re-enqueue): adapt `--audit` TTL-skip; emit ensure TTL; write_prompts reuse; write_team_context TTL; close-asi `[x]`.
  - Industry: [GenerationChangedPredicate](https://github.com/kubernetes-sigs/controller-runtime/blob/main/pkg/predicate/predicate.go) · [GHA lookup-only cache](https://github.com/actions/cache/pull/1041) — skip refresh when generation/tick unchanged.
  - Enqueued `[efficiency-research] Same-tick skip emit+close+prepare in peer_stall_pivot.run_local_pivot_work`. File `scripts/peer_stall_pivot.py`. Distinct from adapt --audit TTL-skip / emit ensure TTL / write_team_context / write_prompts.
- **2026-09-03 (cycle 54 / peer-1)** — `_close_completed_asi_phase_plans` paid gather+rubric every local wake + stall pivot with nothing to close. Hub gather **49.9ms** + rubric **35.6ms** = **85.5ms**; token scan **0.028ms**. Skip unless unchecked WQ has `[asi phase:\w+]` or `[maximize-parallel]` (docs `[asi phase:` without id ignored; factory "maximize parallel task peers" not a close tag). Measure: skip **0.006ms** (~**3050×** vs hub **85.5ms**); 109 automation tests OK. Metric: `latency`. Path: `scripts/peer_loop.py`. Next: write_prompts 4× ASI / stall-pivot adapt `--audit` (queued).
- **2026-09-03 L3 (cycle 54 / hub)** — hub timed probe (`env -u RAM_AUTOMATION_NO_SUBTEST PYTHONPATH=scripts`, wake=30, peers=8, open=18):
  - **`write_team_context` pre-dispatch hot path** — `_mechanical_pre_dispatch` always rebuilds+writes TEAM_CONTEXT (probed **96–216ms**/call; pre_dispatch total **179–287ms**). `TEAM_CONTEXT_MAX_AGE_SEC=3600` only gates `load_team_context_markdown`, not the write path. Immediate re-call still **~96ms** (no bytes-equal skip).
  - Continuous tax (confirm open, do not re-enqueue): emit **83ms** + close-asi **57ms** + prepare **117ms** ≈ **314ms**/wake; shell adapt `--audit` (no `--quick`) **7516ms**; gather×2 in close+prepare; write_prompts **91ms** (asi_progress×2). Witness **0.03ms** (porcelain -u landed).
  - Per-worker yield: every cursor-agent dispatch pays team-context rebuild before plan-gate; at parallel=8 this is serial steal from worker start.
  - Industry: [GenerationChangedPredicate](https://github.com/kubernetes-sigs/controller-runtime/blob/main/pkg/predicate/predicate.go) · [reconcile-loop explosions 2026](https://www.golinuxcloud.com/avoid-kubernetes-operator-reconcile-loop-explosions/) — skip status writes / refreshes when generation unchanged.
  - Enqueued `[efficiency-research] TTL-skip write_team_context on pre-dispatch`. File `scripts/peer_team_context.py`. Distinct from emit TTL / write_prompts / close-asi / stall adapt-audit.
- **2026-09-03 L2 (cycle 54 / hub)** — hub timed probe (`env -u RAM_AUTOMATION_NO_SUBTEST PYTHONPATH=scripts`, wake=30, peers=8, open=12); `should_re_adapt=False` (steady-state):
  - **`run_local_pivot_work` adapt `--audit` hot path** — when fingerprint is current, stall pivot shells `automation_adapt.py --audit` (timeout 90s). Probed **7257ms** (`detect_signals(full)` **6639ms**); `detect_signals(quick)` **4.8ms** (**~1500×**). `stall_pivot_sec=3` → one audit steals the wake and serializes workers.
  - Per-worker yield: Cursor [self-driving](https://cursor.com/blog/self-driving-codebases) 20 agents → 1–3 effective on lock wait; same shape (full probe as keep-working). Industry: [GHA lookup-only](https://github.com/actions/cache/pull/1041) · [Temporal reject-duplicate](https://temporal.io/blog/idempotency-and-durable-execution).
  - Confirm open (do not re-enqueue): close-asi **58.0ms**; write_prompts/`_prepare_continuous_prompts` **203ms**; emit **91ms**. Porcelain `-u` **landed** (`_git_witness` **0.09ms**).
  - Enqueued `[efficiency-research] TTL-skip stall-pivot adapt --audit subprocess`. File `scripts/peer_stall_pivot.py`. Distinct from emit TTL-skip / write_prompts / close-asi / porcelain -u.
- **2026-09-03 (cycle 52 / peer-1 DONE)** — Drop porcelain `-u` from `_git_witness`: removed hub `_git_untracked_count` (`git status --porcelain -u`); mtime-only HEAD+index. Peer-1 ported worktree `gitdir:`+`commondir` ref resolve + `_quick_cache_valid` HIT. Measure: hub witness **2.84→0.031ms**; peer-1 **0.084ms** vs fp **3.88ms** (**46×**); quick HIT **0.19ms**; 104 unittest OK. Metric: `latency`. Paths: hub+peer-1 `scripts/project_automation.py`, peer-1 `tests/test_automation.py`. WQ `[x]`.
- **2026-09-03 L3 (cycle 53 / hub)** — hub timed probe (`env -u RAM_AUTOMATION_NO_SUBTEST PYTHONPATH=scripts`, wake=30, peers=8, open=15):
  - **`_close_completed_asi_phase_plans` hot path** — every `_run_local_continuous` + `peer_stall_pivot.run_local_pivot_work` pays `gather_signals(quick)` **24.5ms** + `compute_asi_rubric` **33.5ms** = **60.1ms**; closed **0** (Active **0** `[asi phase:` / maximize-parallel). WQ token scan **0.17ms** (**354×**).
  - Per-worker yield: stall_pivot_sec=3 → **60ms**/pivot steal; after emit TTL-skip + write_prompts land, this is the remaining keep-working tax with nothing to check off.
  - Confirm open (do not re-enqueue): write_prompts **95.8ms** (reuse partial vs prior 171ms); emit **96–311ms**. porcelain `-u` **[x]** peer-1/hub. gcd subprocess hub **0.85ms** (gitfile `[x]`, hub tip unmerged).
  - Industry: [controller-runtime cache (2026-07-29)](https://kubernetes.io/blog/2026/07/29/controller-runtime-cache-explained/) skip copies for non-matching objects; [GenerationChangedPredicate](https://www.golinuxcloud.com/kubernetes-watches-events-predicates/) drop reconcile when generation unchanged.
  - Enqueued `[efficiency-research] Skip close-asi rubric when no ASI-phase open`. Distinct from emit TTL-skip / write_prompts / porcelain -u.
- **2026-09-03 L2 (cycle 53 / peer-20)** — hub timed probe (`env -u RAM_AUTOMATION_NO_SUBTEST PYTHONPATH=scripts`):
  - **`_git_witness` hot path** — `_git_untracked_count` shells `git status --porcelain -u` (git `-u` alone ⇒ **uall**) on every witness check. Micro: witness **2.14ms**/call vs HEAD/index mtime-only **0.063ms** (**34×**). Miss `measure_live_state(quick)` **216ms** with **4×** porcelain -u + **5×** HEAD + **5×** porcelain; HIT still **1×** -u via `_live_from_quick_cache` → `_quick_cache_valid`.
  - Per-worker yield: `peer_loop` has **11** `measure_live_state` call sites — steady-state HIT tax ≈ **2ms×N**/wake from untracked alone; at wake=20s this is pure steal from dispatch.
  - Industry: [worktrunk#2259](https://github.com/max-sixty/worktrunk/pull/2259) share porcelain across tasks; [liney a1b05a5](https://github.com/everettjf/liney/commit/a1b05a561f5c9e8595637b4aa26fd0fda052d01d) skip untracked scan + mtime cache; ccstatusline#384 HEAD/index mtime git cache.
  - Enqueued `[efficiency-research] Drop porcelain -u from project_automation._git_witness`. Distinct from emit TTL-skip / write_prompts / self-check format_prompt.
- **2026-09-03 (peer-9 DONE)** — `_run_self_check_body`: one `build_plan(quick)` + `loop_work_items` peek; single `format_prompt` for 800-char preview. Skips loop `build_plan` (no `orchestrate_cursor` +3 on verify) + duplicate format (~17ms/gate). Paths: `scripts/peer_orchestrate.py` (hub + peer-9). Distinct from write_prompts / TTL-skip emit.
- **2026-09-03 (cycle 51 / peer-1)** — Landed skip loop `build_plan` + bind `format_prompt` in `run_self_check`. `_self_check_needs_loop_plan` only when `stop && !open_items`. Measure: 12 open, **7.8→4.9ms** (37%); fmt **1.8→0.9ms**; 102 unittest OK. Hub `_run_self_check_body` still dual-builds until vendor/merge.
- **2026-09-03 (cycle 52 / peer-9)** — hub timed probe (`PYTHONPATH=scripts`; peer-9 worktree pruned by pool cap):
  - **`_run_self_check_body` hot path** — `build_plan(quick)` **30.2ms** then `build_plan(quick, loop=True)` **16.8ms**; `format_prompt(plan)` **5.4ms** len=**137077** then called again for `len()` (2nd **0.6ms**). Tax **~53ms** vs reuse-once **~36ms** (**~17ms**/verify gate). Preview only needs **800** chars.
  - Per-worker yield: `build_plan(loop=True)` rotates `orchestrate_cursor` (+3) via `peer_transcript.save_state` on every teacher audit — verify is not a dispatch.
  - Industry: GitHub Actions [parallel steps (2026-06-25)](https://github.blog/changelog/2026-06-25-actions-steps-can-now-be-run-in-parallel/); [Turborepo #12106](https://github.com/vercel/turborepo/pull/12106); k8s `GenerationChangedPredicate`.
  - Enqueued `[efficiency-research] Skip loop build_plan + double format_prompt in peer_orchestrate._run_self_check_body`. Distinct from L3 `write_prompts` / L2 emit TTL-skip.
- **2026-09-03 L3 (cycle 52 / hub)** — peer-31 worktree pruned (excess); probes from hub (`PYTHONPATH=scripts`, wake=30, peers=8, parallel=18). Did **not** re-enqueue L2 TTL-skip ensure (still `[ ]`).
  - **`write_prompts` 4× ASI** — `_prepare_continuous_prompts` → `write_prompts(plan=True, execute=True, combined=True)` then `build_combined_prompt` re-calls `build_plan_prompt`+`build_execute_prompt`; each hits `compute_asi_progress` → `asi_rubric.compute_asi_rubric` (**34.3ms**). Live: write_prompts **171ms**, prepare **197ms**/wake, gather_signals **21ms**, close-asi **58ms**. After L2 emit skip (~93ms), this is the remaining hot path.
  - Land: build plan+execute once, concat for combined; skip `write_text` when bytes match ([GenerationChangedPredicate](https://github.com/kubernetes-sigs/controller-runtime/blob/master/pkg/predicate/predicate.go) / [reconcile-loop explosions 2026](https://www.golinuxcloud.com/avoid-kubernetes-operator-reconcile-loop-explosions/)). File `scripts/automation_improve.py`.
  - Confirm emit: `_emit_worktree_inventory` **95.4ms** vs list **2.1ms** (~**45×**); gcd subprocess hub **1.48ms** vs gitfile **0.013ms** (already `[x]`, hub tip unmerged).
  - Enqueued `[efficiency-research] Reuse plan/execute strings in automation_improve.write_prompts`.
- **2026-09-03 L2 (cycle 51 / peer-20)** — timed probes on hub (`PYTHONPATH=scripts`, pool target=8, max_parallel_peers=8):
  - **`_emit_worktree_inventory` hot path** — full ensure+list **116.2ms**/tick; list-only **2.3ms**; save **~114ms**/continuous wake. `ensure_parallel_pool` alone **~89–95ms** with **26×** `_git_common_dir` + **11×** `list_worktrees`; 8× `ensure_coding_worktree` (already exist) **36.5ms** (each pays primary×2 via `hub_path`).
  - Subprocess gcd still on hub tip: hub **1.56ms**/call, slot **1.35ms**; gitfile parse (peer-1) **0.026–0.108ms** (match) — prior enqueue; not re-enqueued.
  - Per-worker yield: at wake=20s, ensure tax ≈ **5.8%** of wake window with zero pool work; TTL/ready-skip returns that budget to dispatch.
  - Side note: excess prune (`N >= max`) force-removes research lanes like peer-20 on every ensure — keep research slots ≤cap or TTL-skip so probes do not self-evict.
  - Enqueued `[efficiency-research] TTL-skip ensure in peer_loop._emit_worktree_inventory`.
- **2026-09-03 (cycle 50)** — Stale `dgx_utilization` fill/pool=48 after `max_parallel_peers=8`: peer-1 `dgx_speed.local.json:92,96` still 48 (hub overlay already 8); hub `automation.config.local.json` fill/pool=48; install `cp` overlay→local re-injects. Clamp in `dgx_utilization.target_agent_fill`/`worktree_pool_size` = `min(raw, peers)` (vendored to peer-1). Shrink overlay+local to 8. Measure: live CFG fill **48→8**; `target_agent_fill()`/`worktree_pool_size()` **8**; stale-48 sim still clamps to 8; `_nudge_agents` target=8 at_cap. Metric: `wake` / per-worker yield (no chase of 48 hub slots). Paths: `scripts/dgx_speed.local.json`, `scripts/dgx_utilization.py`, `tests/test_dgx_utilization.py`.
- **2026-09-03 L3 (cycle 49 / hub)** — timed probes after peer-31 excess prune (`env -u RAM_AUTOMATION_NO_SUBTEST PYTHONPATH=scripts python3 …` from hub):
  - **`Parse gitfile in _git_common_dir`** — ensure(no post) **31.1ms** list=**8** gcd=**17** primary=**17** add=**0**; gitfile patch **17.9ms** (**13.2ms**); slot×8 **1.658→0.037ms** (**45×**, match); hub **0.758→0.031ms** (**24×**); enqueued.
  - Prior 48-cap probe (before prune): ensure **304.3ms** → **202.7ms** (**~101ms**); gcd=**97**.
  - Industry: Turborepo [#12106](https://github.com/vercel/turborepo/pull/12106) replaced `rev-parse --git-common-dir` with filesystem gitdir reads (~4.7ms fork tax/call).
  - Per-worker yield: at floor=8 gcd is ~42% of ensure(no post); scales linearly with pool size.
